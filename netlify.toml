# OwanbeX — Vite SPA. Publish "dist". Mock-by-default until Supabase is wired.
[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA deep-link fallback for react-router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
